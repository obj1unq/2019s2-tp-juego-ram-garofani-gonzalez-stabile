import personajes.*
import config.*
import wollok.game.*
import niveles.*

program RaM {

    game.title("RaM")
    game.height(14)
    game.width(13)
    niveles.presentar()
    //game.showAttributes(rick) //Debug


    config.teclas()
    config.ticks()
    config.configurarColisiones()
    game.start()
}
