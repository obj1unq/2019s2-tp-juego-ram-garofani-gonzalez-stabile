import personajes.*
import config.*
import wollok.game.*
import niveles.*

program RaM {
	
	game.title("RaM")
	game.height(10)
	game.width(10)
	multiverso.enVista(1)
	multiverso.add(rick)
	multiverso.add(gun)
	gun.position(multiverso.at(1,1,1))

	game.showAttributes(rick) //Debug 	 


    config.teclas()

	game.start()
}
