import rick.*
import config.*
import wollok.game.*
import niveles.*

program RaM {

    game.title("RaM")
    game.height(14)
    game.width(13)
    niveles.presentar()
    config.teclas()
    config.configurarColisiones()
    game.start()
}
