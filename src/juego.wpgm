import personajes.*
import config.*
import wollok.game.*
import niveles.*

program RaM {
	
	game.title("RaM")
	game.height(10)
	game.width(10)
	universo.enVista(1)
	universo.add(rick)
	universo.add(gun)
	gun.position(universo.at(1,1,1))

	game.showAttributes(rick) //Debug 	 


    config.teclas()

	game.start()
}
