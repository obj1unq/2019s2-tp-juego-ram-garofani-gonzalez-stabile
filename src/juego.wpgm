import personajes.*
import config.*
import wollok.game.*
import niveles.*

program RaM {
	
	game.title("RaM")
	game.height(10)
	game.width(10)
	multiverso.enVista(1)
	multiverso.add(rick)
	gun.position(game.at(1,9), multiverso.enVista())
	multiverso.add(gun)

	game.showAttributes(rick) //Debug 	 


    config.teclas()

	game.start()
}
